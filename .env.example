# ── Socket.io server environment variables ──────────────────────────────────
# Copy to .env and fill in your values.

# Server port (default: 8080)
PORT=8080

# Admin panel secret — required to enable the /admin namespace.
# Must match VITE_ADMIN_SECRET in the game client's .env.local.
ADMIN_SECRET=

# Supabase project credentials (server-side, service_role key).
# Enables DB writes for coop_sessions, flash_quiz_results, loot_events, finale_sessions.
# Leave empty to run in memory-only mode.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
